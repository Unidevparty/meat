<?php

return <<<'VALUE'
"namespace IPS\\Theme;\n\tfunction email_html_core_notification_new_comment( $comment, $email ) {\n\t\t$return = '';\n\t\t$return .= <<<CONTENT\n\n\n\nCONTENT;\n\n$item = $comment->item();\n$return .= <<<CONTENT\n\n\n\nCONTENT;\n\nif ( $comment->author()->member_id ):\n$return .= <<<CONTENT\n\n{$email->language->addToStack(\"email_new_comment\", FALSE, array( 'sprintf' => array( $comment->author()->url(), $comment->author()->name, $item->indefiniteArticle( $email->language ) ) ) )} <a href='{$comment->item()->url()}'>{$comment->item()->mapped('title')}<\/a>\n\nCONTENT;\n\nelseif ( $comment->author()->real_name ):\n$return .= <<<CONTENT\n\n{$email->language->addToStack(\"email_new_comment_guest\", FALSE, array( 'sprintf' => array( $email->language->addToStack( 'guest_name_shown', NULL, array( 'sprintf' => array( $comment->author()->real_name ) ) ), $item->indefiniteArticle( $email->language ) ) ) )} <a href='{$comment->item()->url()}'>{$comment->item()->mapped('title')}<\/a>\n\nCONTENT;\n\nelse:\n$return .= <<<CONTENT\n\n{$email->language->addToStack(\"email_new_comment_guest\", FALSE, array( 'sprintf' => array( $email->language->addToStack( 'guest_name_shown', NULL, array( 'sprintf' => array( $email->language->addToStack('guest') ) ) ), $item->indefiniteArticle( $email->language ) ) ) )} <a href='{$comment->item()->url()}'>{$comment->item()->mapped('title')}<\/a>\n\nCONTENT;\n\nendif;\n$return .= <<<CONTENT\n\n\n<br \/>\n<br \/>\n\n{$comment->emailContent( $email, 'html' )}\n<br \/><br \/>\n<em style='color: #8c8c8c'>&mdash; \nCONTENT;\n\n$return .= \\IPS\\Settings::i()->board_name;\n$return .= <<<CONTENT\n<\/em>\nCONTENT;\n\n\t\treturn $return;\n}\n\tfunction email_plaintext_core_notification_new_comment( $comment, $email ) {\n\t\t$return = '';\n\t\t$return .= <<<CONTENT\n\n\n\nCONTENT;\n\n$item = $comment->item();\n$return .= <<<CONTENT\n{$email->language->addToStack(\"email_new_comment_plain\", FALSE, array( 'htmlsprintf' => array( $comment->author()->name, $item->indefiniteArticle( $email->language ) ) ) )} {$comment->item()->mapped('title')}\n\n{$comment->emailContent( $email, 'plaintext' )}\n\n-- \nCONTENT;\n\n$return .= \\IPS\\Settings::i()->board_name;\n$return .= <<<CONTENT\n\nCONTENT;\n\n\t\treturn $return;\n}"
VALUE;
